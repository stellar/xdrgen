[package]
name = "rs-xdr-types"
version = "0.0.0"
edition = "2021"

[dependencies]
base64 = "0.13.0"
arbitrary = { version = "1.1.3", features = ["derive"] }

[features]
default = ["std"]
std = ["alloc", "base64/std"]
alloc = []
